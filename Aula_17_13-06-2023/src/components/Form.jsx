export const Form = ({ children }) => {
    return (
        <form>
            <label>Nome:</label>
            <input type="text" alt="name" />
            {children}
        </form>
        //Kened, Cauê e Arthur
    )
}